# PI-Light 前端功能对接文档

> 本文档面向后端开发同事，说明前端近期新增/完善的三大功能模块的 API 对接细节。

---

## 一、2D 交通仿真视图

### 功能概述

在「仿真监控」页面，使用 Three.js 正交相机渲染 2D 交通仿真视图，包含：
- 道路车道线条（灰色）
- 路口节点（深灰圆点）
- 信号灯指示器（横排绿·黄·红三灯，深色方框背景，当前相位灯亮起）
- 车辆三角形（蓝色=行驶中，黄色=停止，顶角朝前进方向）

### 依赖 API

#### 1. 获取路网拓扑

```
GET /api/datasets/{dataset_id}/roadnet
```

**响应格式：**

```json
{
  "nodes": [
    { "id": "node_1", "x": 100.0, "y": 200.0, "type": "traffic_light" | "priority" | ... }
  ],
  "edges": [
    {
      "id": "edge_1",
      "from": "node_1",
      "to": "node_2",
      "lanes": [
        { "id": "edge_1_0", "shape": [[x1,y1], [x2,y2], ...] }
      ]
    }
  ],
  "traffic_lights": [
    { "id": "node_1" }
  ],
  "bounds": {
    "xMin": 0, "yMin": 0, "xMax": 1000, "yMax": 1000
  }
}
```

**要点：**
- 数据来源：解析 `.net.xml` 文件，有内存缓存
- `bounds` 用于自动缩放相机视野
- `traffic_lights` 列表用于绘制信号灯指示器
- 前端在进入监控页时调用一次，后续不再重复请求

#### 2. 获取实时仿真数据

```
GET /api/experiments/{exp_id}/monitor
```

**响应中与仿真视图相关的字段：**

```json
{
  "vehicles": [
    { "id": "veh_0", "x": 150.0, "y": 300.0, "angle": 90.0, "speed": 8.5 }
  ],
  "signals": [
    { "id": "node_1", "phase": "绿" | "黄" | "红", "remaining": 15 }
  ]
}
```

**要点：**
- `vehicles`：`angle` 为 SUMO 角度（0=北/上，顺时针增大），前端换算为方向向量 `dx=sin(θ), dy=-cos(θ)`
- `signals`：`phase` 为中文"绿/黄/红"，前端据此点亮对应灯色
- 前端每 500ms 轮询一次此接口
- 运行中的实验：数据来自 `shared_state` 实时写入
- 已完成的实验：数据从 `detail_{exp_id}.json` 文件读取

#### 3. 前端渲染逻辑

| 元素 | 渲染方式 | 颜色 |
|---|---|---|
| 道路车道 | `THREE.Line` + `lane.shape` 坐标 | `#3b4a5e` |
| 路口节点 | `THREE.CircleGeometry` | `#4a5568` |
| 信号灯节点 | `THREE.CircleGeometry`（稍大） | `#10b981` |
| 信号灯指示器 | `THREE.PlaneGeometry`（方框）+ 3× `CircleGeometry`（灯） | 方框 `#1a1a2e`，灯灭 `#222244`，灯亮按相位 |
| 行驶车辆 | 等腰三角形 `BufferGeometry`，顶角朝前进方向 | `#3b82f6`（蓝） |
| 停止车辆 | 同上 | `#f59e0b`（黄） |

---

## 二、已保存配置管理

### 功能概述

在「实验管理」页面，新增「已保存配置」栏目，支持：
- 查看所有已保存的实验配置
- 一键使用保存的配置重新训练
- 删除不需要的配置

### 依赖 API

#### 1. 获取配置列表

```
GET /api/configs/
```

**响应格式：**

```json
[
  {
    "id": "eaa166cd",
    "filename": "config_eaa166cd.yaml",
    "datasetName": "Atlanta",
    "modelName": "frap",
    "createdAt": 1779437354.5,
    "config": { ... }
  }
]
```

**要点：**
- `id` 是文件名中 `config_` 后面的部分（即实验 ID 的前缀）
- `datasetName` 从 `config.environment.config.roadnet_file` 路径中提取（取 `data/` 后的子目录名）
- `modelName` 从 `config.algorithm` 中提取 `plugin` 字段
- `createdAt` 是文件修改时间戳（`st_mtime`），前端通过 `formatTime()` 转为可读格式
- 配置文件存储位置：`PI-Light/results/config_{id}.yaml`

#### 2. 使用配置重新训练

```
POST /api/configs/{config_id}/retrain?gui_enabled=false
```

**响应格式：**

```json
{
  "experiment_id": "new_uuid",
  "message": "使用保存的配置重新训练已启动"
}
```

**要点：**
- 内部调用 `experiments._start_experiment_with_config()`
- 实验名称格式：`retrain_{config_id}`
- 训练完成后会生成新的 `config_{new_exp_id}.yaml` 到 results 目录

#### 3. 删除配置

```
DELETE /api/configs/{config_id}
```

**响应格式：**

```json
{
  "success": true,
  "message": "配置文件已删除"
}
```

**要点：**
- 同时删除 `config_{config_id}.yaml` 和 `detail_{config_id}.json`（如果存在）
- 配置不存在时返回 404

### 配置文件保存时机

实验完成时，`experiment_service.py` 的 `run_training()` 函数自动将完整配置写入：

```python
config_path = PROJECT_ROOT / "results" / f"config_{exp_id}.yaml"
yaml.dump(full_config, f, allow_unicode=True, sort_keys=False)
```

---

## 三、AI 智能分析对比结果

### 功能概述

在「对比分析」页面，接入 DeepSeek LLM Agent，支持：
- 自动分析选中实验的指标对比结果
- 多轮追问对话
- 流式输出（打字机效果）
- Markdown 格式渲染（标题、加粗、代码块、表格等）

### 依赖 API

#### 1. 检查 Agent 可用性

```
GET /api/agent/status
```

**响应格式：**

```json
{
  "available": true,
  "model": "deepseek-chat",
  "base_url": "https://api.deepseek.com/v1"
}
```

**要点：**
- `available` 取决于 `LLM_API_KEY` 是否已配置
- 前端在发起分析前先调用此接口，不可用时提示用户配置 API Key

#### 2. 发起 AI 分析（SSE 流式）

```
POST /api/agent/analyze
Content-Type: application/json
```

**请求 Body：**

```json
{
  "experiment_ids": ["exp_id_1", "exp_id_2"],
  "metrics": ["ATT", "AQL"],
  "question": "可选，自定义问题，首次分析可不传",
  "history": [
    {"role": "user", "content": "..."},
    {"role": "assistant", "content": "..."}
  ]
}
```

**响应格式：** `text/event-stream`（SSE 流式）

每条数据为 JSON 行：

```json
{"content": "根据实验"}
{"content": "数据分析，"}
{"content": "ATT指标..."}
```

或错误：

```json
{"error": "LLM API 调用失败: ..."}
```

**要点：**
- 使用 DeepSeek API（OpenAI 兼容格式），流式输出
- 后端自动构建实验上下文，包含：
  - 实验名称、数据集、模型
  - 各项指标数值
  - 奖励曲线趋势（后 1/3 均值 vs 前 1/3 均值，阈值 10%）
- 若未提供 `question`，自动生成分析请求
- `history` 支持多轮对话上下文，前端维护对话历史

### 前端 Markdown 渲染策略

| 阶段 | 处理方式 | 说明 |
|---|---|---|
| 流式接收中 | 显示 HTML 转义后的纯文本 | 避免不完整 Markdown 被错误解析产生破损 HTML |
| 流式接收中样式 | `white-space: pre-wrap` + 等宽字体 | 打字机效果 |
| 流结束后 | 一次性调用 `marked.parse(fullContent)` | 完整 Markdown → HTML 渲染 |
| 渲染后样式 | 标题/加粗/代码/表格/引用等均有独立 CSS | 暗色主题适配 |

**关键：不要在流式过程中反复调用 `marked.parse()`**，不完整的 Markdown 会产生未闭合标签、残缺表格等问题。

### LLM 配置

位于 `app/config.py`：

```python
LLM_API_KEY = "sk-xxx"           # DeepSeek API Key
LLM_BASE_URL = "https://api.deepseek.com/v1"
LLM_MODEL = "deepseek-chat"
```

---

## 四、前端轮询与数据刷新

| 页面 | 轮询接口 | 间隔 | 触发条件 |
|---|---|---|---|
| 实验管理 | `GET /api/experiments/?status=completed` + `?status=running` + `GET /api/configs/` | 5s | 页面激活时启动，离开时停止 |
| 仿真监控 | `GET /api/experiments/{id}/monitor` | 500ms | 进入监控页时启动，离开时停止 |
| 对比分析 | 无轮询 | - | 用户手动触发 |

---

## 五、关键路径常量

| 常量 | 路径 | 说明 |
|---|---|---|
| `PROJECT_ROOT` | `PI-Light/` | 项目根目录 |
| `DATA_DIR` | `PI-Light/data/` | 数据集目录 |
| `RESULTS_DIR` | `PI-Light/results/` | 实验结果 + 配置文件 |
| `CONFIGS_DIR` | `PI-Light/configs/` | 临时运行配置（训练后自动清理） |
| `INDEX_FILE` | `PI-Light/results/index.json` | 实验索引 |

---

## 六、常见问题

**Q: 已保存配置显示 No Data？**
A: 确认 `results/` 目录下存在 `config_*.yaml` 文件，且 `RESULTS_DIR` 路径正确指向 `PI-Light/results/`。

**Q: 仿真视图空白？**
A: 确认 `GET /api/datasets/{dataset_id}/roadnet` 返回了有效的 `nodes`/`edges`/`bounds` 数据。数据集目录下需要有 `.net.xml` 文件。

**Q: AI 分析报错？**
A: 检查 `app/config.py` 中的 `LLM_API_KEY` 是否有效，以及网络是否能访问 `https://api.deepseek.com/v1`。

**Q: reward_curve 格式不一致？**
A: 运行中时 `reward_curve` 可能是对象数组 `[{avg_reward: 1.2, ...}, ...]`，已结束时是纯数值数组 `[1.2, ...]`。前端需兼容两种格式。
