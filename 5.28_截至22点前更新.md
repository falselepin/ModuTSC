# 5.28 截至22点前更新

---

## 一、快速开始模式修复

**问题**：快速开始模式无法正常启动实验，原来仅设置数据集并跳转页面，用户还需手动操作。

**修改**：[test3.0.html quickStart()](file:///d:/桌面/ModuTSC/test3.0.html#L1822-L1841) — 重写为直接通过 `/api/experiments/start` 启动实验，使用固定组件配置：

| 组件类型 | 组件名 |
|----------|--------|
| 编排器 | single |
| 算法 | frap |
| 动作 | phase |
| 观测 | frap |
| 收集器 | replay |
| 奖励 | composite |
| 环境 | sumo |

启动后自动跳转到实验管理页面。

---

## 二、算法维度不匹配修复（核心修复）

**问题**：快速开始和自由装配实验均报错 `mat1 and mat2 shapes cannot be multiplied (64x22 and 8x64)`。

**根因**：Observer 和 Algorithm 使用不同方式计算观测维度：
- **FRAP Observer**：`max(所有路口phase数) + max(所有路口lanelink数)` → 正确的全局维度
- **DQN Algorithm**：`cfg.get("act_in_dim", 8)` → 硬编码默认值 8，完全错误
- **FRAP Algorithm bind_topology**：仅用第一个路口的 `len(gp)` → 多路口时维度小于 Observer

### 2.1 DQN 算法重构

**文件**：[modutsc/plugins/algorithms/drl/dqn.py](file:///d:/桌面/ModuTSC/modutsc/plugins/algorithms/drl/dqn.py)

| 改动 | 说明 |
|------|------|
| `setup()` | 从 env 计算 `obs_dim = act_dim + num_lanelinks`，与 Observer 一致 |
| 新增 `bind_topology()` | 从 env 重新计算维度，变化时自动重建网络 |
| 新增 `reinitialize_with_obs_dim()` | 供编排器在试观测后校正 obs_dim |
| 新增 `_reinit_networks()` | 统一重建 Q-net / target-net / optimizer |
| `act()` | mask 与 Q-values 长度不一致时截断对齐，避免 `ValueError` |

### 2.2 FRAP 算法 bind_topology 修复

**文件**：[modutsc/plugins/algorithms/drl/frap.py](file:///d:/桌面/ModuTSC/modutsc/plugins/algorithms/drl/frap.py#L96-L135)

**修改**：`bind_topology()` 改为 `max(所有路口的phase数)` 和 `max(所有路口的lanelink数)` 计算维度，与 FRAP Observer 保持一致。原来仅使用第一个路口的参数，多路口场景下维度不足。

### 2.3 Single 编排器 obs_dim 兜底校正

**文件**：[modutsc/orchestration/single.py](file:///d:/桌面/ModuTSC/modutsc/orchestration/single.py#L89-L109)

**修改**：`_bind_algorithms_topology()` 增加 obs_dim 校正逻辑 — 在所有算法 `bind_topology()` 之后，执行一次试观测，检查每个算法的 `_obs_dim` 是否与实际观测维度一致，不一致则调用 `reinitialize_with_obs_dim()` 重建网络。

**三层防御机制**：

| 层级 | 时机 | 机制 |
|------|------|------|
| 第一层 | 算法 `setup()` | 从 env 计算 obs_dim |
| 第二层 | `bind_topology()` | 从 env 重新计算并校正 |
| 第三层 | 编排器兜底 | 试观测后检查实际维度，不匹配则重建 |

---

## 三、Tracker 实时指标修复

**问题**：仿真监控界面实时指标全部显示 n/a（ATT=n/a; AQL=n/a; Throughput=n/a），后端终端也无指标输出。

### 3.1 Tracker 配置缺失 sumo_episode_kpis

**文件**：[modutsc/api.py scaffold_config()](file:///d:/桌面/ModuTSC/modutsc/api.py#L1437-L1440)

**根因**：`scaffold_config` 生成的 tracker 配置为 `{"plugin": "console", "config": {}}`，空 config 导致 `ConsoleTracker.setup({})` 中 `sumo_episode_kpis=False`，交通 KPI 追踪器 `_traffic=None`，`episode_kpi_dict()` 返回空字典。

**修改**：tracker config 改为 `{"sumo_episode_kpis": True}`，确保交通 KPI 追踪器正确初始化。

### 3.2 Orchestrator.run() 指标输出被跳过

**文件**：[modutsc/orchestration/__init__.py](file:///d:/桌面/ModuTSC/modutsc/orchestration/__init__.py#L196-L216)

**根因**：当 tracker 存在时（`tr is not None`），下方所有 `print()` 指标输出被 `if tr is None:` 条件跳过，导致后端终端看不到任何 ATT/AQL/Throughput 等指标。

**修改**：移除 `if tr is None:` 条件，每个 episode 结束后始终打印指标到终端。

### 3.3 自定义编排器 test_single 添加 Tracker 集成

**文件**：[test_single.py](file:///d:/桌面/ModuTSC/test_single.py)

**修改**：
- `episode()` 开始时调用 `tracker.reset_episode_stats(env)`
- 每步调用 `tracker.accumulate_step(env)`
- 每 5 步通过 traci 采集车辆位置和信号灯状态写入 `_shared_state`（供前端仿真视图渲染）
- 返回值包含 `**kpi` 展开交通 KPI
- 结束时调用 `tracker.log()`
- `evaluate()` 同步添加 tracker 调用

---

## 四、自定义插件安全白名单扩展

**文件**：[modutsc/scheduling/dynamic_loader.py](file:///d:/桌面/ModuTSC/modutsc/scheduling/dynamic_loader.py#L32)

**修改**：`_ALLOWED_MODULES` 新增 `traci`。

**原因**：自定义编排器需要在 episode 循环中调用 `traci.vehicle.getIDList()` / `traci.vehicle.getPosition()` 等接口采集车辆位置和信号灯状态，供前端 2D 仿真视图渲染。`traci` 是 SUMO 仿真环境的标准 Python API，属于安全模块。

---

## 五、配置约束面板修复

### 5.1 约束面板无法展示

**文件**：[modutsc/api.py get_orchestrator_kinds()](file:///d:/桌面/ModuTSC/modutsc/api.py#L109-L113)

**根因**：`get_orchestrator_kinds()` 返回的 kinds 列表不包含 `"environment"`，导致 `ConstraintSession` 初始化时 `ConfigSolver` 没有 `environment` 的 `_values` 字典，`solver.solve()` 遍历等式时访问 `self._values["environment"]` 抛出 `KeyError`。

**修改**：与 `launcher.py` 同步，始终将 `"environment"` 追加到 kinds 列表。

### 5.2 约束显示语义优化

**文件**：[test3.0.html](file:///d:/桌面/ModuTSC/test3.0.html) + [modutsc/api.py](file:///d:/桌面/ModuTSC/modutsc/api.py#L770)

| 改动 | 修改前 | 修改后 |
|------|--------|--------|
| 维度匹配标签 | `observer.observe ↔ actor.translate` | `维度匹配: observer.observe → actor.translate` |
| 已匹配状态 | ✅ 已匹配 (22 = 22) | ✅ ✓ 已匹配 |
| 冲突状态 | ⚠️ 不匹配 (8 ≠ 22) | ❌ ✗ 维度冲突 |
| 待配置状态 | 🔲 待解析 | ⏳ 等待配置 |
| 内部参数未设置 | 待定 | ⏳ 待配置 |
| 底部提示 | 🔲 待解析约束参数：X 个 | ⏳ 需要配置：X 个参数待设置 |
| 全部满足 | ✅ 约束关系已满足 | ✅ 所有约束已满足 ✓ |

**改进说明**：
- 双箭头 `↔` 改为单箭头 `→`，明确表示数据流方向（输出→输入）
- 添加"维度匹配:"前缀，明确约束类型
- `⏳` 替代 `🔲`，更直观地表达"等待中"语义
- `❌` 替代 `⚠️`，更明确地表达冲突/错误

---

## 六、自定义插件模板新增 tracker 类型

**文件**：[test3.0.html pluginKindOptions](file:///d:/桌面/ModuTSC/test3.0.html#L1094-L1102)

**修改**：新增 `{ label: '追踪器 tracker', value: 'tracker' }` 选项。

**原因**：后端 `scaffold.py` 已完整支持 tracker（在 `_KNOWN_ABC` 中），但前端缺少入口，用户无法通过自定义插件界面生成 tracker 模板。

---

## 修改文件汇总

| 文件 | 修改类型 | 涉及问题 |
|------|----------|----------|
| `test3.0.html` | 修改 | 快速开始、约束显示优化、tracker 模板选项 |
| `modutsc/api.py` | 修改 | get_orchestrator_kinds 修复、scaffold_config tracker 配置、约束标签优化 |
| `modutsc/plugins/algorithms/drl/dqn.py` | 修改 | setup 从 env 计算 obs_dim、bind_topology、reinitialize_with_obs_dim、act mask 安全处理 |
| `modutsc/plugins/algorithms/drl/frap.py` | 修改 | bind_topology 使用 max 跨所有路口、act mask 安全处理 |
| `modutsc/orchestration/single.py` | 修改 | _bind_algorithms_topology 增加 obs_dim 兜底校正 |
| `modutsc/orchestration/__init__.py` | 修改 | run() 移除 if tr is None 条件，始终打印指标 |
| `modutsc/scheduling/dynamic_loader.py` | 修改 | _ALLOWED_MODULES 新增 traci |
| `test_single.py` | 修改 | episode/evaluate 添加 tracker 集成 + 车辆采集 |
