# 5.26 截至22点前更新

---

## 一、SSE 流式响应丢字修复

**问题**：对比分析 AI 智能分析内容完全不显示。

**根因**：`app/routers/agent.py` 中 `stream_response()` 生成器每次 `yield` JSON 字符串时没有加 `\n`，多个 JSON 对象在同一个 TCP chunk 中粘连，前端 `split('\n')` 后无法解析。

**修改**：[app/routers/agent.py](file:///d:/桌面/ModuTSC/app/routers/agent.py) — 4 处 `yield` 全部加上 `+ "\n"`。

---

## 二、SSE 前端行缓冲修复

**问题**：流式响应偶尔丢字（如 `HangZhou1` 变成 `Hang`）。

**根因**：SSE 数据按 chunk 分块传输，一个 JSON 行可能被切成两半跨 chunk，前端直接 `split('\n')` 会导致尾部不完整的半行被丢弃。

**修改**：[test3.0.html](file:///d:/桌面/ModuTSC/test3.0.html) `_callAgent` 函数 — 新增 `lineBuffer` 变量缓存跨 chunk 的不完整行，每次收到新 chunk 先拼接再 split，流结束后处理残留数据。

---

## 三、2D 仿真小车朝向修复

**问题**：复杂路网（如洛杉矶）上小车三角形图标朝向不正确。

**根因**：直接使用 API 返回的 `angle` 字段，在弯道路网上该角度与实际行驶方向不一致。

**修改**：[test3.0.html](file:///d:/桌面/ModuTSC/test3.0.html) `updateVehicles` 函数 — 新增 `prevVehicles` 缓存上一帧车辆位置，通过相邻两帧位移计算实际运动方向（`atan2(moveDx, -moveDy)`），首帧回退到 `angle` 字段。路网切换和视图销毁时清理缓存。

---

## 四、功能四：自定义插件上传（完整实现）

### 4.1 后端

| 文件 | 操作 | 说明 |
|------|------|------|
| `modutsc/scheduling/dynamic_loader.py` | 新增 | 动态加载器：AST 安全白名单校验、接口契约检查、`importlib` 动态导入、插件索引管理 |
| `modutsc/scheduling/registry.py` | 修改 | 新增 `register_dynamic()` 运行时注册函数、`discover_user_plugins()` 委托函数 |
| `app/routers/plugins.py` | 新增 | 6 个 API 端点：上传 / 列表 / 详情 / 删除 / 验证 / 模板生成 |
| `app/main.py` | 修改 | 启动时调用 `discover_user_plugins()` + 注册新路由 + 重建兼容性缓存 |

### 4.2 前端

| 改动 | 说明 |
|------|------|
| 导航栏 | 新增 "📦 自定义插件" 入口 |
| 自定义插件页面 | 左右两栏：上传表单（类型/名称/描述/拖拽上传）+ 插件列表 + 代码模板生成器 |
| 自由装配集成 | `fetchModules()` 末尾自动拉取用户插件，合并到模块货架（标记 `source:'user'`） |
| CSS | 新增 `form-item`、`plugin-upload-zone`、`plugin-code-preview` 样式 |

### 4.3 安全机制

- AST 级别白名单检查，禁止 `import os/sys/subprocess` 等危险模块
- 禁止 `eval/exec/compile/__import__` 调用
- 禁止文件写入操作
- 插件名与内置插件冲突检测

---

## 五、编排器兼容性修复

**问题**：上传自定义编排器后，其他组件（算法/观测等）全部无法选中。

**根因**：模板生成的 `setup(self, cfg: dict, env: Optional["Env"] = None)` 中 `env` 参数被系统误解析为组件种类（`environment`），导致 `get_orch_kinds()` 只返回 `["environment"]`，兼容性矩阵缺少其他组件。

**修改**：
- [registry.py](file:///d:/桌面/ModuTSC/modutsc/scheduling/registry.py) `get_orch_kinds()` — 排除 `env` 参数
- [registry.py](file:///d:/桌面/ModuTSC/modutsc/scheduling/registry.py) `_derive_orch_attr_map()` — 同上排除 `env`

---

## 六、模板生成器：继承抽象基类 + 复用 scaffold

**问题**：最初模板生成的是裸类（无基类），方法签名硬编码，与框架基类不一致。

**修改过程**：

1. **第一版**：裸类 + `*args, **kwargs` 通用签名 → 系统识别不了组件依赖
2. **第二版**：手动硬编码每种方法的签名和基类映射 → 可用但维护成本高，基类改了模板不跟着变
3. **最终版**：直接复用 `modutsc/scheduling/scaffold.py` 中已有的 `_load_abc` / `_scan_abc` / `_generate_code` 三个函数，通过 `inspect` 自动扫描基类的 `@abstractmethod` 生成签名

最终 `scaffold_plugin_template` 从 130+ 行缩减到 10 行，基类怎么改模板就怎么生成，永远保持一致。

---

## 七、安全白名单扩展

[dynamic_loader.py](file:///d:/桌面/ModuTSC/modutsc/scheduling/dynamic_loader.py) `_ALLOWED_IMPORTS` 新增：

- `modutsc.plugins` 及其子模块（algorithms / observers / actors / collectors / rewards / trackers）
- `modutsc.orchestration`

允许用户插件 `from modutsc.plugins.algorithms import Algorithm` 等合法导入。
